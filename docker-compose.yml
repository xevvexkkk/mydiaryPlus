version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: mydiary_db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in the project .env file}
      POSTGRES_DB: ${DB_NAME:-mydiary}
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-mydiary}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  mydiary:
    build: .
    container_name: mydiary_app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in the project .env file}
      DB_NAME: ${DB_NAME:-mydiary}
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in the project .env file}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads
      - ./storage:/app/storage
      - ./.env:/app/backend/.env:ro
    restart: unless-stopped
